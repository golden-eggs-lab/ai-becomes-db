# SCIP Requirements
# Core dependencies for reproducing the SCIP paper experiments

# Deep Learning Frameworks
# Note: Install PyTorch first separately with CUDA support:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
transformers>=4.35.0
accelerate>=0.24.0
datasets>=2.14.0
peft>=0.7.0  # For LoRA efficient fine-tuning

# Code Models and Evaluation
# bigcode/starencoder for embeddings
# codellama models for fine-tuning
sentencepiece>=0.1.99
protobuf>=3.20.0

# Data Processing
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Evaluation Benchmarks
# For HumanEval and MBPP evaluation
openai  # For HumanEval dataset format

# Experiment Tracking and Visualization
wandb>=0.15.0
tensorboard>=2.14.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Optional: For faster training (install manually after PyTorch)
# flash-attn>=2.3.0  # Requires CUDA, install with: pip install flash-attn --no-build-isolation
# bitsandbytes>=0.41.0  # For 8-bit quantization

# Optional: For distributed training
# deepspeed>=0.10.0  # Alternative to FSDP

# Code Quality and Testing
pytest>=7.4.0
black>=23.0.0
isort>=5.12.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
python-dotenv>=1.0.0
